#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int debug;

	debug = 0;
	positive_or_negative(debug);

	return (0);
}
