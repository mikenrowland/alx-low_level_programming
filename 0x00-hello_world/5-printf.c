#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/*printing to the stdout using printf*/
	printf("with proper grammer, but the outcome is a piece of art,");

	return (0);
}
